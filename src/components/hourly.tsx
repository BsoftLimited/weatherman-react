import { Hourly } from "../utils/types";

interface HourlyViewProps{
    hourly: Hourly
}

const HourlyView:React.FC<HourlyViewProps> = ({hourly}) =>{
    return (
        <div>

        </div>
    );
}

export default HourlyView;